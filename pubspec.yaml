name: anmolverma_in
description: Anmol Verma ~ Full Stack Mobile Application Developer

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  google_fonts: ^2.1.0
  url_launcher: ^6.0.2
  mailto: ^2.0.0
  http: ^0.13.3
  metadata_fetch:
    git: https://github.com/Anmol92verma/metadata_fetch


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - packages/anmolverma_in/assets/mutualmobile.jpeg
    - packages/anmolverma_in/assets/svimtech.png
    - packages/anmolverma_in/assets/trestor.png
    - packages/anmolverma_in/assets/xicom.png
